[tool.coverage.run]
branch = true
source = [ "wealth_management_portal_email_sender_mcp" ]

[tool.coverage.report]
exclude_lines = [ "if TYPE_CHECKING:" ]
show_missing = true

[tool.pytest.ini_options]
addopts = """
 --cov
 --cov-report html:'../../coverage/packages/email_sender_mcp/html'
 --cov-report xml:'../../coverage/packages/email_sender_mcp/coverage.xml'
 --html='../../reports/packages/email_sender_mcp/unittests/html/index.html'
 --junitxml='../../reports/packages/email_sender_mcp/unittests/junit.xml'
 --ignore=tests/integration
"""
markers = [
  "integration: marks tests as integration tests (deselect with '-m \"not integration\"')",
]

[tool.hatch.build.targets.wheel]
packages = [ "wealth_management_portal_email_sender_mcp" ]

[tool.hatch.metadata]
allow-direct-references = true

[tool.ruff]
exclude = [ ".ruff_cache", ".svn", ".tox", ".venv", "dist" ]
line-length = 120
indent-width = 4

  [tool.ruff.lint]
  select = [ "E", "F", "UP", "B", "SIM", "I" ]
  ignore = [ ]
  fixable = [ "ALL" ]
  unfixable = [ ]

[project]
name = "wealth_management_portal.email_sender_mcp"
version = "1.0.0"
description = "Automatically generated by Nx."
requires-python = ">=3.12"
readme = "README.md"
dependencies = [
  "aws-lambda-powertools==3.24.0",
  "aws-lambda-powertools[tracer]==3.24.0",
  "aws-lambda-powertools[parser]==3.24.0",
  "boto3>=1.34.0",
  "markdown>=3.7",
]

[build-system]
requires = [ "hatchling" ]
build-backend = "hatchling.build"

[dependency-groups]
dev = [
  "moto[s3,ses]>=5.0.0",
  "python-dotenv>=1.1.0",
]
