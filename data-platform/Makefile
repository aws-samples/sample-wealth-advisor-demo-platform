# Bootstrap Makefile — only provides the init target.
# Running 'make init' generates the full Makefile from Makefile.template.
# After init, all other make targets become available.

SHELL := /usr/bin/env bash -euo pipefail -c

init:
	./init.sh
